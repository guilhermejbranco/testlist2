{
  "name": "egghead-react",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://guilhermejbranco.github.io/testlist2",
  "devDependencies": {
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  }
}
